/**
 * ContactsBooking
 * Main entry point that imports the modular ContactsBooking components
 */

import ContactsBooking from "./ContactsBooking/index";

export default function ContactsBookingMain() {
  return <ContactsBooking />;
}
